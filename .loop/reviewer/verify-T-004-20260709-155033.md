VERDICT: FAIL

src/lib/tools/get_signals.ts:230 · high · `getSignals` loads any `target_accounts` row by `accountId` and proceeds to call Sillage without checking `account.market`; because the D1 migration has no DB `CHECK` for `jp|kr|sg`, an out-of-scope account row can trigger paid Sillage calls and persisted signals, violating T-004’s “JP/KR/SG accounts only” criterion and AGENTS.md invariant 12.

drizzle/0002_target-accounts.sql:4 · high · The persisted `target_accounts.market` column is plain `text NOT NULL` with no database constraint; the TypeScript enum in `src/lib/db/app.schema.ts:27` is not sufficient to enforce Phase 1 scope once data is in D1. Add a DB constraint or a runtime guard before external calls.

tickets/T-004-tool-get-signals.md:80 · high · The live verification note records “0 signals” and run `#1036` with “0 detections”; this does not satisfy the acceptance criterion at `tickets/T-004-tool-get-signals.md:32` requiring a live call to return real signals for at least one Q-4 account. Documenting an honest empty is fine for invariant 2, but the criterion remains unmet unless the ticket is blocked or re-tested with a real signal.

VERDICT: FAIL
